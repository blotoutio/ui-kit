import styled from 'styled-components'
import { neutrals60, white } from '../common/colors'

export const Wrapper = styled.div`
  color: ${neutrals60};
  position: relative;
  background: ${white};
`
